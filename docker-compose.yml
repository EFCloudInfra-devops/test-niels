
services:
  backend:
    build: ./backend
    container_name: ex4300-backend
    ports:
      - "8000:8000"
    env_file:
      - backend/.env
    volumes:
      - ./backend/data:/app/data
      - ./secrets:/app/keys:ro
  frontend:
    build: ./frontend
    container_name: ex4300-frontend
    ports:
      - "8080:80"
    depends_on:
      - backend
